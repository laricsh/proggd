import * as React from "react";

interface Props {
  name: string;
}

const MainGraphPanel = (props: Props) => {
  return (
    <div>
      <p>Main Graph</p>
    </div>
  );
};
export default MainGraphPanel;
